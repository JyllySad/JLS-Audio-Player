<UserControl x:Class="JLS.Views.PlayHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#B2000000" UseLayoutRounding="True" SnapsToDevicePixels="True">
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Opacity" Value="0"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOpen}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsHitTestVisible">
                                        <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                    </BooleanAnimationUsingKeyFrames>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseIn"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsHitTestVisible">
                                        <DiscreteBooleanKeyFrame KeyTime="0:0:0:0.15" Value="False"/>
                                    </BooleanAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <Border Background="Transparent">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding CloseHistoryCommand}"/>
            </Border.InputBindings>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2.5*" MinWidth="450" MaxWidth="700"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="6*" MinHeight="400" MaxHeight="850"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="1" Grid.Row="1" CornerRadius="12" Padding="0">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{x:Null}"/>
                </Border.InputBindings>

                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#252528"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding DataContext.IsGlassModeEnabled, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#1E1E1E" Opacity="0.85"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.7" ShadowDepth="0"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="25,25,25,10">
                        <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="White" Margin="0,-10,0,0" Text="Listening History" VerticalAlignment="Center"/>

                        <Button Background="Transparent" BorderThickness="0" Command="{Binding CloseHistoryCommand}" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" RenderTransformOrigin="0.5,0.5" Text="&#xE106;" x:Name="IconText">
                                        <TextBlock.Foreground>
                                            <SolidColorBrush Color="#888888" x:Name="IconBrush"/>
                                        </TextBlock.Foreground>
                                        <TextBlock.RenderTransform>
                                            <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                                        </TextBlock.RenderTransform>
                                    </TextBlock>

                                    <ControlTemplate.Triggers>
                                        <EventTrigger RoutedEvent="MouseEnter">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="IconText" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.3"/>
                                                    <DoubleAnimation Duration="0:0:0.15" Storyboard.TargetName="IconText" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.3"/>
                                                    <ColorAnimation Duration="0:0:0.15" Storyboard.TargetName="IconBrush" Storyboard.TargetProperty="Color" To="#FFFFFF"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MouseLeave">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetName="IconText" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0"/>
                                                    <DoubleAnimation Duration="0:0:0.25" Storyboard.TargetName="IconText" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0"/>
                                                    <ColorAnimation Duration="0:0:0.25" Storyboard.TargetName="IconBrush" Storyboard.TargetProperty="Color" To="#888888"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <ListBox AlternationCount="9999" Background="Transparent" BorderThickness="0" Grid.Row="1" ItemsSource="{Binding Tracks}" Margin="10,0,10,20" Padding="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" VirtualizingPanel.IsVirtualizing="False" VirtualizingPanel.ScrollUnit="Pixel" VirtualizingPanel.VirtualizationMode="Recycling">

                        <ListBox.OpacityMask>
                            <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                                <GradientStop Color="Transparent" Offset="0.0"/>
                                <GradientStop Color="Black" Offset="0.05"/>
                                <GradientStop Color="Black" Offset="0.95"/>
                                <GradientStop Color="Transparent" Offset="1.0"/>
                            </LinearGradientBrush>
                        </ListBox.OpacityMask>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontSize="12" Foreground="#666666" HorizontalAlignment="Center" Margin="0,25,0,15" Text="History of the last 200 tracks" Visibility="Collapsed" x:Name="HistoryHeader"/>

                                    <Border CornerRadius="10" Margin="0,0,0,8" Padding="15,12">
                                        <Border.Style>
                                            <Style BasedOn="{StaticResource FadeInLoadedStyle}" TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1AFFFFFF"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Grid>
                                            <Grid Visibility="Visible" x:Name="SingleTrackLayout">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Margin="0,0,15,0" VerticalAlignment="Center">
                                                    <TextBlock FontSize="16" FontWeight="Bold" Foreground="White" Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#A0A0A0" Margin="0,2,0,0" Text="{Binding Artist}" TextTrimming="CharacterEllipsis"/>

                                                    <StackPanel Margin="0,4,0,0" Orientation="Horizontal">
                                                        <TextBlock FontSize="12" Foreground="#777777" MaxWidth="220" Text="{Binding Album}" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock FontSize="12" Foreground="#555555" Margin="4,0" Text=" • "/>
                                                        <TextBlock FontSize="12" Foreground="#777777" Text="{Binding Year}"/>
                                                    </StackPanel>

                                                    <TextBlock FontSize="12" Foreground="#666666" Margin="0,4,0,0" Text="{Binding ListenDate}"/>
                                                </StackPanel>

                                                <Border Background="#28282B" CornerRadius="8" Grid.Column="1" Height="68" VerticalAlignment="Center" Width="68">
                                                    <Grid>
                                                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#555555" HorizontalAlignment="Center" Text="&#xE189;" VerticalAlignment="Center"/>
                                                        <Border CornerRadius="8" Tag="{Binding Cover, NotifyOnTargetUpdated=True}">
                                                            <Border.Background>
                                                                <ImageBrush ImageSource="{Binding Cover}" Stretch="UniformToFill"/>
                                                            </Border.Background>
                                                            <Border.Triggers>
                                                                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                                    <BeginStoryboard>
                                                                        <Storyboard>
                                                                            <DoubleAnimation Duration="0:0:0.35" From="0" Storyboard.TargetProperty="Opacity" To="1">
                                                                                <DoubleAnimation.EasingFunction>
                                                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                                                </DoubleAnimation.EasingFunction>
                                                                            </DoubleAnimation>
                                                                        </Storyboard>
                                                                    </BeginStoryboard>
                                                                </EventTrigger>
                                                            </Border.Triggers>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </Grid>

                                            <StackPanel Visibility="Collapsed" x:Name="AlbumGroupLayout">
                                                <TextBlock FontSize="12" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,8" Text="ALBUM"/>

                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Background="#28282B" CornerRadius="8" Grid.Column="0" Height="68" Margin="0,0,14,0" VerticalAlignment="Center" Width="68">
                                                        <Grid>
                                                            <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="#555555" HorizontalAlignment="Center" Text="&#xE189;" VerticalAlignment="Center"/>
                                                            <Border CornerRadius="8" Tag="{Binding Cover, NotifyOnTargetUpdated=True}">
                                                                <Border.Background>
                                                                    <ImageBrush ImageSource="{Binding Cover}" Stretch="UniformToFill"/>
                                                                </Border.Background>
                                                                <Border.Triggers>
                                                                    <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                                        <BeginStoryboard>
                                                                            <Storyboard>
                                                                                <DoubleAnimation Duration="0:0:0.35" From="0" Storyboard.TargetProperty="Opacity" To="1">
                                                                                    <DoubleAnimation.EasingFunction>
                                                                                        <QuadraticEase EasingMode="EaseOut"/>
                                                                                    </DoubleAnimation.EasingFunction>
                                                                                </DoubleAnimation>
                                                                            </Storyboard>
                                                                        </BeginStoryboard>
                                                                    </EventTrigger>
                                                                </Border.Triggers>
                                                            </Border>
                                                        </Grid>
                                                    </Border>

                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock FontSize="18" FontWeight="Bold" Foreground="White" Text="{Binding AlbumTitle}" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock FontSize="14" Foreground="#A0A0A0" Margin="0,2,0,0" Text="{Binding AlbumArtist}"/>
                                                    </StackPanel>
                                                </Grid>

                                                <ItemsControl ItemsSource="{Binding AlbumTracks}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,4,0,6">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="32"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>

                                                                <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="#888888" Grid.Column="0" HorizontalAlignment="Left" Text="{Binding TrackNumber}" VerticalAlignment="Center"/>

                                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                    <TextBlock FontSize="14" FontWeight="SemiBold" Foreground="White" Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                                                                    <TextBlock FontSize="12" Foreground="#666666" Margin="0,2,0,0" Text="{Binding ListenDate}"/>
                                                                </StackPanel>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </StackPanel>

                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsAlbumGroup}" Value="True">
                                        <Setter Property="Visibility" TargetName="SingleTrackLayout" Value="Collapsed"/>
                                        <Setter Property="Visibility" TargetName="AlbumGroupLayout" Value="Visible"/>
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=(ItemsControl.AlternationIndex)}" Value="0">
                                        <Setter Property="Visibility" TargetName="HistoryHeader" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border CornerRadius="12" Grid.RowSpan="2" IsHitTestVisible="False">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.IsGlassModeEnabled, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Setter Property="BorderThickness" Value="1.5"/>

                                        <Setter Property="BorderBrush">
                                            <Setter.Value>
                                                <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                                                    <GradientStop Color="#44FFFFFF" Offset="0.0"/>
                                                    <GradientStop Color="#0AFFFFFF" Offset="0.15"/>
                                                    <GradientStop Color="#00FFFFFF" Offset="1.0"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>

                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                                                    <GradientStop Color="#15FFFFFF" Offset="0.0"/>
                                                    <GradientStop Color="#08FFFFFF" Offset="0.15"/>
                                                    <GradientStop Color="#04FFFFFF" Offset="0.5"/>
                                                    <GradientStop Color="#00FFFFFF" Offset="1.0"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>